import { Proximamente } from '@/components/proximamente';

export default function RankingsPage() {
  return (
    <Proximamente
      titulo="Rankings"
      descripcion="La tabla completa de la comunidad. El top semanal ya se ve en el Inicio."
    />
  );
}
