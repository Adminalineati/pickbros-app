import { Proximamente } from '@/components/proximamente';

export default function PerfilPage() {
  return (
    <Proximamente
      titulo="Perfil"
      descripcion="Tu cuenta, rango y logros. Hoy los datos del header son de demostración."
    />
  );
}
