import { Proximamente } from '@/components/proximamente';

export default function NotificacionesPage() {
  return (
    <Proximamente
      titulo="Notificaciones"
      descripcion="Avisos de rachas, retos y canjes. Todavía no hay un sistema de push."
    />
  );
}
