import { Proximamente } from '@/components/proximamente';

export default function AnalisisPage() {
  return (
    <Proximamente
      titulo="Análisis"
      descripcion="Lecturas y contexto de los duelos. Se conectará cuando exista contenido real."
    />
  );
}
