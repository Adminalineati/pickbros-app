import { Proximamente } from '@/components/proximamente';

export default function RetosPage() {
  return (
    <Proximamente
      titulo="Retos"
      descripcion="Desafíos entre Picksters. Aún no está la lógica, solo el lugar en el menú."
    />
  );
}
